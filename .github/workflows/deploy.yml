name: DISABLED: Legacy deploy (use deploy-canonical)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  notice:
    name: This workflow is disabled
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled. Use .github/workflows/deploy-canonical.yml (tags v* or manual ref)."