# 🚨 КРИТИЧЕСКИЕ ПРАВИЛА ДЕПЛОЯ

## 🔧 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### React 18 vs React 19 конфликт
- Проект использует **React 18.3.1**
- Библиотека `react-leaflet@5.0.0` требует **React 19**
- **РЕШЕНИЕ**: всегда использовать `--legacy-peer-deps` при установке
- В workflow уже настроено: `npm config set legacy-peer-deps true`
- **НЕ ОБНОВЛЯЙ react-leaflet** без проверки совместимости!

---

## ⛔ ЗАПРЕЩЕНО АГЕНТАМ

### 1. НИКОГДА не удалять эти файлы/папки:
```
src/                    # Исходный код React
public/                 # Статические файлы
package.json            # Зависимости проекта
package-lock.json       # Lock-файл зависимостей
index.html              # Точка входа
vite.config.ts          # Конфиг сборки
tsconfig.json           # Конфиг TypeScript
tailwind.config.ts      # Конфиг Tailwind
.github/workflows/      # CI/CD пайплайны
```

### 2. НИКОГДА не изменять workflow файлы без явного запроса:
- `.github/workflows/deploy-canonical.yml` — **ЕДИНСТВЕННЫЙ ПРАВИЛЬНЫЙ ДЕПЛОЙ**
- **ОТКЛЮЧЁННЫЕ (неправильные) workflow файлы:**
  - `deploy-on-command.yml` — DISABLED (отключён)
  - `deploy.yml` — DISABLED (legacy)
  - `deploy-to-pages-root.yml` — DISABLED (legacy)
  - `deploy-release.yml` — DISABLED (legacy)
- **НЕ ИСПОЛЬЗУЙ эти отключённые workflow!** Только `deploy-canonical.yml`!

### 3. НИКОГДА не делать `git reset --hard` без понимания последствий

---

## ✅ КАК ПРАВИЛЬНО ДЕПЛОИТЬ

### Единственный правильный способ:
```bash
git add <файлы>
git commit -m "описание"
git push origin main
```
Деплой запустится **автоматически** через `deploy-canonical.yml`

### Что делает deploy-canonical.yml:
1. `npm config set legacy-peer-deps true` — решает конфликт React 18/19
2. `npm ci --legacy-peer-deps` — устанавливает зависимости
3. `npm run build` — собирает проект
4. Деплоит на GitHub Pages

### ⚠️ НЕ ИСПОЛЬЗУЙ другие workflow файлы для деплоя!

---

## 🔍 ПЕРЕД ЛЮБЫМ КОММИТОМ ПРОВЕРЬ

```bash
# Сколько файлов в репозитории?
git ls-files | wc -l
# Должно быть ~1000+ файлов!

# Что будет закоммичено?
git status

# НЕ удаляются ли критические файлы?
git diff --stat
```

---

## 🆘 ЕСЛИ ДЕПЛОЙ СЛОМАЛСЯ

1. Найди последний успешный деплой:
   https://github.com/johnda7/island-travel-echo-clone/actions?query=is%3Asuccess

2. Откатись к нему:
   ```bash
   git reset --hard <commit-hash>
   git push origin main --force
   ```

3. Последний известный рабочий коммит: `be6cb12`

---

## 📋 ЧТО СЛУЧИЛОСЬ 19.12.2025

Агент случайно удалил 1140 файлов из репозитория в коммите `56ff611`.
В результате деплой падал с "exit code 254" потому что не было исходников.

**Решение:** откат до `be6cb12` через `git reset --hard`
