# 📊 Аналитика групповых чатов - Telegram Bot

## 🎯 Что это?

Бот подключается к группе **"ПХУКЕТ DA 🇹🇭"** и **тихо наблюдает** за разговорами, собирая статистику:
- Сколько сообщений в день
- Какие туры обсуждают чаще всего
- Сколько вопросов о турах
- Кто активные участники

**НЕ СПАМИТ** - не отвечает на каждое сообщение, только собирает данные!

---

## ✅ Что уже работает:

### 1. Сбор статистики (LIVE)

Бот отслеживает:
- 💬 **Количество сообщений** в группе
- 👥 **Уникальных пользователей** (Set - без дубликатов)
- 🏝️ **Упоминания туров** по ключевым словам:
  - Maya Bay, Phi Phi, James Bond, Similan, Bamboo Island
  - Coral Island, Banana Beach, Racha Island, Khai Islands
  - Promthep Cape, Cheow Lan Lake
- ❓ **Вопросы** (ключевые слова: цена, стоимость, сколько, когда, где, как, booking)

### 2. Команды для админа

```
/stats - Показать статистику группы
```

**Пример вывода:**
```
📊 Статистика группы за 24 часа:

💬 Сообщений: 143
👥 Уникальных пользователей: 42
❓ Вопросов: 18

🔥 Топ туров:
1. Maya Bay: 12 упоминаний
2. Phi Phi: 8 упоминаний
3. James Bond: 5 упоминаний
4. Similan: 3 упоминания
5. Cheow Lan Lake: 2 упоминания

📅 Период: с 05.11.2025
```

```
/resetstats - Сбросить статистику (начать заново)
```

### 3. Логирование в консоль

Каждое сообщение логируется:
```
📊 [ПХУКЕТ "DA" 🇹🇭 | ЧАТ] Ольга: "кто был на Phi Phi?"
📊 [ПХУКЕТ "DA" 🇹🇭 | ЧАТ] Иван: "рекомендую Maya Bay на рассвете"
```

Это видно в логах Koyeb (Console).

---

## 🔧 Как это работает (техническая часть):

### Код в `welcome-message.js`:

```javascript
// Объект для хранения статистики
const groupStats = {
  messages: 0,
  tourMentions: {
    'Maya Bay': 0,
    'Phi Phi': 0,
    'James Bond': 0,
    // ... и т.д.
  },
  users: new Set(),  // Set - автоматически убирает дубликаты!
  questions: 0,
  lastReset: new Date()
};

// Ключевые слова для анализа
const keywords = {
  questions: ['цена', 'стоимость', 'сколько', 'когда', 'где', 'как'],
  tours: {
    'Maya Bay': ['maya', 'майя', 'бухта майя'],
    'Phi Phi': ['phi phi', 'пхи пхи', 'фи фи'],
    // ... и т.д.
  }
};

// Обработчик ВСЕХ сообщений в группе
bot.on('message', async (ctx) => {
  const chatType = ctx.chat.type;
  const text = ctx.message.text.toLowerCase();
  
  // Только в группах
  if (chatType === 'group' || chatType === 'supergroup') {
    groupStats.messages++;
    groupStats.users.add(ctx.from.id);
    
    // Анализируем упоминания туров
    Object.entries(keywords.tours).forEach(([tourName, tourKeywords]) => {
      if (tourKeywords.some(keyword => text.includes(keyword))) {
        groupStats.tourMentions[tourName]++;
      }
    });
    
    // Считаем вопросы
    if (keywords.questions.some(keyword => text.includes(keyword))) {
      groupStats.questions++;
    }
    
    // Логируем
    console.log(`📊 [${ctx.chat.title}] ${ctx.from.first_name}: "${text.substring(0, 50)}..."`);
    
    return; // НЕ отвечаем на каждое сообщение!
  }
});
```

---

## ❌ Чего НЕТ (но можно добавить):

### 1. Постоянное хранение данных
**Проблема:** Данные хранятся в оперативной памяти (переменная `groupStats`). При рестарте бота - данные обнуляются.

**Решение (TODO):**
- Добавить Supabase для хранения статистики
- Или Google Sheets API
- Или даже простой JSON-файл

### 2. Еженедельные отчёты
**Что хочется:** Каждую неделю бот автоматически отправляет админу отчёт:
```
📊 Недельный отчёт (01.11 - 08.11):

💬 Всего сообщений: 1043
👥 Активных пользователей: 124
🔥 Самые обсуждаемые туры:
   1. Phi Phi (42 упоминания)
   2. Maya Bay (38 упоминаний)
   3. James Bond (27 упоминаний)

💡 Рекомендации:
- Создать пост про Phi Phi (активный интерес!)
- Ответить на вопросы про Maya Bay (много вопросов)
```

**Решение (TODO):**
- `node-cron` для запуска задач по расписанию
- Сохранять данные перед отправкой отчёта

### 3. AI-анализ настроений
**Что хочется:** Бот понимает не только ЧТО говорят, но и КАК:
- Позитивные отзывы ("супер тур!", "amazing!")
- Негативные ("разочарован", "не понравилось")
- Вопросы без ответов (никто не помог человеку)

**Решение (TODO):**
- Интеграция с OpenAI GPT API
- Анализ sentiment (позитив/негатив)
- Алерты админу если появились негативные отзывы

### 4. Автоответы на частые вопросы
**Что хочется:** Если человек спрашивает "сколько стоит Phi Phi?", бот отвечает приватно (не в группе!):
```
👋 Привет! Вижу ты интересуешься Phi Phi.

Вот информация:
💰 Цена: от 6500฿ (2 дня/1 ночь)
📱 Забронировать: t.me/phuketgos_bot?start=phiphi2days

Есть вопросы? Пиши! 😊
```

**Решение (TODO):**
- Детектировать вопросы в группе
- Отправлять ответ в ЛС (не спамить группу)
- Добавить debounce (не отвечать на каждое сообщение одного человека)

---

## 🚀 Roadmap для аналитики:

### Фаза 1: Постоянное хранение (3-4 часа)
- [ ] Интеграция с Supabase
- [ ] Таблицы: `group_stats`, `tour_mentions`, `user_activity`
- [ ] Автосохранение каждые 5 минут
- [ ] Восстановление данных при рестарте бота

### Фаза 2: Еженедельные отчёты (2-3 часа)
- [ ] `node-cron` для расписания
- [ ] Генерация отчёта (текст + графики?)
- [ ] Отправка админу каждый понедельник 9:00
- [ ] Дополнительно: отчёт по команде `/report`

### Фаза 3: AI-анализ (5-7 часов)
- [ ] OpenAI GPT API интеграция
- [ ] Sentiment analysis (позитив/негатив/нейтрал)
- [ ] Детекция вопросов без ответов
- [ ] Алерты админу при негативных отзывах
- [ ] Саммари обсуждений ("О чём говорили сегодня?")

### Фаза 4: Умные автоответы (4-5 часов)
- [ ] NLP для детекции вопросов
- [ ] База частых вопросов (FAQ)
- [ ] Ответы в ЛС (не спамить группу)
- [ ] Debounce (1 ответ в 5 минут на человека)
- [ ] A/B тестирование эффективности ответов

---

## 💡 Примеры использования:

### 1. Контент-стратегия
**Вопрос:** "О каких турах писать посты в соцсетях?"  
**Ответ:** Смотрим статистику → видим что Phi Phi обсуждают чаще всего → делаем пост про Phi Phi!

### 2. Улучшение описаний туров
**Вопрос:** "Какие вопросы задают про Maya Bay?"  
**Ответ:** Анализируем тексты → видим "сколько стоит" и "как добраться" → добавляем эту инфу в описание тура.

### 3. Выявление проблем
**Вопрос:** "Почему мало бронирований Similan?"  
**Ответ:** Смотрим статистику → видим что про Similan вообще не говорят → значит надо больше продвигать!

### 4. Персонализация
**Вопрос:** "Кто самые активные пользователи?"  
**Ответ:** Смотрим топ-10 → предлагаем им скидку за лояльность или просим оставить отзыв.

---

## 📞 Как посмотреть статистику:

### Вариант 1: Через бота
1. Открой группу "ПХУКЕТ DA 🇹🇭"
2. Напиши `/stats`
3. Получи отчёт!

### Вариант 2: Через логи Koyeb
1. Открой https://app.koyeb.com/
2. Перейди в Services → island-travel-echo-clone
3. Открой вкладку **Console**
4. Увидишь все логи: `📊 [ПХУКЕТ "DA" 🇹🇭 | ЧАТ] ...`

---

## ⚙️ Настройка (для разработчика):

### Добавить новый тур в отслеживание:

```javascript
// В welcome-message.js
const groupStats = {
  tourMentions: {
    'Maya Bay': 0,
    'Новый Тур': 0,  // ← добавить сюда
    // ...
  }
};

const keywords = {
  tours: {
    'Maya Bay': ['maya', 'майя'],
    'Новый Тур': ['новый', 'new tour', 'новенький'],  // ← и сюда
    // ...
  }
};
```

### Добавить новое ключевое слово для вопросов:

```javascript
const keywords = {
  questions: [
    'цена', 'стоимость', 'сколько',
    'доступно',  // ← добавить сюда
    'свободно'   // ← и ещё одно
  ]
};
```

---

## 🆘 Troubleshooting:

### Бот не собирает статистику:
1. Проверь что бот добавлен в группу
2. Проверь что у бота есть права читать сообщения
3. Посмотри логи в Koyeb Console

### Статистика обнулилась:
- Бот перезапускался (это нормально для текущей версии)
- Решение: добавить Supabase (см. Фаза 1 roadmap)

### Команда /stats не работает:
- Проверь что пишешь команду в группе (не в ЛС)
- Проверь Chat ID группы (должен быть `-1002449303081`)

---

**Последнее обновление:** 9 ноября 2025  
**Статус:** ✅ Работает, но хранение временное (в памяти)  
**Приоритет улучшения:** Средний (сначала Deep Links и бронирования)
