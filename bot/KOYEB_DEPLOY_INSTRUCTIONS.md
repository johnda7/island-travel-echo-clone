# 🚀 Инструкция по деплою бота на Koyeb

## ✅ Что исправлено

1. ✅ Добавлен **Express** сервер для стабильной работы
2. ✅ Переключен с **polling** на **webhook** режим
3. ✅ Настроены **health check** endpoints (`/` и `/health`)
4. ✅ Исправлен **Dockerfile** с правильным портом 8000
5. ✅ Добавлен **graceful shutdown**

## 📋 Настройка в Koyeb Dashboard

### 1️⃣ Environment Variables (Переменные окружения)

В Koyeb добавьте эти переменные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
PORT=8000
WEBHOOK_DOMAIN=ваш-домен.koyeb.app
NODE_ENV=production
```

**Важно**: `WEBHOOK_DOMAIN` должен совпадать с доменом вашего сервиса в Koyeb (без https://)

Пример:
```
WEBHOOK_DOMAIN=small-robinia-phukeo-8b5e1e16.koyeb.app
```

### 2️⃣ Service Settings

- **Port**: `8000`
- **Health check path**: `/health`
- **Health check port**: `8000`
- **Start command**: `npm start` (или оставьте пустым - Dockerfile сам запустит)

### 3️⃣ Instance Type

Минимальные требования:
- **Nano** (0.25 vCPU, 512MB RAM) - для старта
- **Micro** (0.5 vCPU, 1GB RAM) - рекомендуется для стабильности

### 4️⃣ Build Settings

- **Build method**: Dockerfile
- **Dockerfile path**: `bot/Dockerfile` (если деплоите из корня репо)
- **Context**: `/` (корень репозитория)

## 🔄 Как обновить деплой

### Вариант 1: Через Git (рекомендуется)

```bash
cd /Users/evgeniymikhelev/island-travel-echo-clone-9

# Коммит изменений
git add bot/
git commit -m "fix: переключил бот на webhook режим для Koyeb"
git push origin main

# Koyeb автоматически задеплоит обновление
```

### Вариант 2: Force Redeploy в Koyeb

1. Откройте ваш сервис в Koyeb
2. Нажмите **"Redeploy"** 
3. Выберите **"Force redeploy"**

## 🧪 Тестирование после деплоя

### 1. Проверьте health check

```bash
curl https://ваш-домен.koyeb.app/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "bot": "running",
  "uptime": 123.45,
  "timestamp": "2025-11-10T..."
}
```

### 2. Проверьте главную страницу

Откройте в браузере: `https://ваш-домен.koyeb.app/`

Должна отобразиться страница с информацией о боте.

### 3. Проверьте webhook в Telegram

```bash
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo
```

Должен показать:
```json
{
  "ok": true,
  "result": {
    "url": "https://ваш-домен.koyeb.app/telegram-webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### 4. Протестируйте бот

Напишите боту в Telegram: `/start`

Бот должен ответить приветственным сообщением.

## 🐛 Troubleshooting

### Ошибка: "Webhook failed"

**Причина**: Неправильный `WEBHOOK_DOMAIN`

**Решение**: 
1. Зайдите в Koyeb → Services → ваш сервис
2. Скопируйте домен (например: `small-robinia-phukeo-8b5e1e16.koyeb.app`)
3. Обновите переменную `WEBHOOK_DOMAIN` в Settings → Environment Variables
4. Сделайте Redeploy

### Ошибка: "Health check failed"

**Причина**: Порт не совпадает

**Решение**:
- Убедитесь что в Environment Variables: `PORT=8000`
- Убедитесь что в Service Settings → Health check port: `8000`

### Бот не отвечает

**Причина**: Webhook не установлен

**Решение**:
1. Проверьте логи в Koyeb (Logs tab)
2. Найдите строку: `✅ Webhook установлен: https://...`
3. Если ошибка - проверьте `WEBHOOK_DOMAIN`

### Deployment failed

**Причина**: Ошибка сборки Docker

**Решение**:
1. Проверьте логи деплоя в Koyeb
2. Убедитесь что все файлы закоммичены в Git
3. Проверьте что `bot/package.json` содержит `express` в dependencies

## 📝 Полезные команды

### Локальное тестирование (webhook режим)

```bash
cd bot
npm install

# Установите переменные окружения
export BOT_TOKEN="ваш_токен"
export PORT=8000
export WEBHOOK_DOMAIN="localhost:8000"

# Запустите бота
npm start
```

**Примечание**: Для локального тестирования webhook нужен ngrok или похожий тоннель.

### Локальное тестирование (polling режим)

Если хотите протестировать локально без webhook, временно замените последние строки в `welcome-message.js`:

```javascript
// Временно для локального тестирования
bot.launch().then(() => {
  console.log('✅ Бот запущен в polling режиме (локально)');
});
```

**Важно**: Не коммитьте это в Git! Koyeb требует webhook режим.

## 🎯 Ожидаемый результат

После успешного деплоя:

- ✅ Сервис показывает статус "Running" в Koyeb
- ✅ Health check `/health` возвращает 200 OK
- ✅ Webhook установлен на `https://ваш-домен.koyeb.app/telegram-webhook`
- ✅ Бот отвечает на команды в Telegram
- ✅ Логи показывают: "✅ Webhook установлен"

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Koyeb Dashboard → Logs
2. Проверьте переменные окружения
3. Убедитесь что BOT_TOKEN правильный (от @BotFather)

---

**Версия**: 2025-11-10  
**Режим**: Webhook (production-ready для Koyeb)  
**Статус**: ✅ Готово к деплою
