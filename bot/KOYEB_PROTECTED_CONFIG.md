# 🔒 ЗАЩИЩЁННАЯ КОНФИГУРАЦИЯ KOYEB - НЕ МЕНЯТЬ!

**⚠️ КРИТИЧЕСКИ ВАЖНО ДЛЯ AI АГЕНТА:**
Этот файл описывает рабочую конфигурацию бота на Koyeb.
**НЕ МЕНЯЙ** эти файлы без крайней необходимости!

---

## 🛡️ ЗАЩИЩЁННЫЕ ФАЙЛЫ (НЕ ТРОГАТЬ!)

### 1️⃣ `bot/Dockerfile` - КРИТИЧЕСКИ ВАЖЕН
```dockerfile
# РАБОЧАЯ КОНФИГУРАЦИЯ - НЕ МЕНЯТЬ CMD!
FROM node:20-alpine
ENV BUILD_DATE=YYYY-MM-DDTHH:MM:SS  # ← Только это можно менять для force rebuild
WORKDIR /app
COPY bot/package*.json ./
RUN npm install --production
COPY bot/ ./
EXPOSE 8000
CMD ["node", "smart-ai-bot.js"]  # ← НЕ МЕНЯТЬ! Это рабочий бот!
```

**❌ ЗАПРЕЩЕНО:**
- Менять `CMD` на другой файл
- Менять порт `8000`
- Удалять `EXPOSE 8000`

**✅ РАЗРЕШЕНО:**
- Обновлять `BUILD_DATE` для force rebuild

---

### 2️⃣ `bot/package.json` - КРИТИЧЕСКИ ВАЖЕН
```json
{
  "main": "smart-ai-bot.js",  // ← НЕ МЕНЯТЬ!
  "scripts": {
    "start": "node welcome-message.js",  // ← НЕ МЕНЯТЬ!
    "start:ai": "node smart-ai-bot.js"   // ← НЕ МЕНЯТЬ!
  }
}
```

**❌ ЗАПРЕЩЕНО:**
- Менять `main`
- Менять `start` скрипт
- Удалять зависимости

**✅ РАЗРЕШЕНО:**
- Добавлять новые скрипты (например `start:new`)
- Добавлять новые зависимости

---

### 3️⃣ `bot/smart-ai-bot.js` - ОСНОВНОЙ БОТ
Это рабочий бот который запускается на Koyeb.

**❌ ЗАПРЕЩЕНО:**
- Удалять файл
- Ломать webhook endpoint `/telegram-webhook`
- Ломать health endpoint `/health`
- Менять порт (должен быть `process.env.PORT || 8000`)

**✅ РАЗРЕШЕНО:**
- Добавлять новые команды
- Улучшать обработчики
- Добавлять новые туры

---

## 🚀 КАК ПРАВИЛЬНО ДЕПЛОИТЬ НА KOYEB

### Автоматический деплой (РЕКОМЕНДУЕТСЯ):
```bash
# 1. Внеси изменения в код бота
# 2. Обнови BUILD_DATE для сброса кэша (ТОЛЬКО ЭТО!)
sed -i '' "s/BUILD_DATE=.*/BUILD_DATE=$(date +%Y-%m-%dT%H:%M:%S)/" bot/Dockerfile

# 3. Commit и push
git add bot/
git commit -m "fix: обновление бота"
git push origin main

# 4. Koyeb АВТОМАТИЧЕСКИ задеплоит через 2-3 минуты!
```

### Проверка статуса деплоя:
```bash
# Health check - должен вернуть {"status":"ok","bot":"running"}
curl https://small-robinia-phukeo-8b5e1e16.koyeb.app/health
```

---

## 📋 ЧЕКЛИСТ АГЕНТА ПОСЛЕ ИЗМЕНЕНИЙ БОТА

После ЛЮБЫХ изменений в папке `bot/`:

1. ✅ **НЕ менял** `Dockerfile` CMD
2. ✅ **НЕ менял** `package.json` main/start
3. ✅ **Обновил** только `BUILD_DATE` в Dockerfile
4. ✅ **Сделал** `git push origin main`
5. ✅ **Подождал** 2-3 минуты
6. ✅ **Проверил** health: `https://small-robinia-phukeo-8b5e1e16.koyeb.app/health`
7. ✅ **Написал** пользователю статус:

```
✅ KOYEB ДЕПЛОЙ УСПЕШЕН:
- Health check: OK
- Bot: running
- Uptime: [X] сек (перезапустился)
```

---

## 🔗 ВАЖНЫЕ ССЫЛКИ

- **Koyeb Dashboard:** https://app.koyeb.com
- **Health Check:** https://small-robinia-phukeo-8b5e1e16.koyeb.app/health
- **Webhook Domain:** small-robinia-phukeo-8b5e1e16.koyeb.app
- **Telegram Bot:** @phuketgos_bot
- **Порт:** 8000

---

## ⚠️ ЕСЛИ БОТ СЛОМАЛСЯ

1. **Проверь логи** на Koyeb Dashboard → Logs
2. **Верни рабочую версию:**
```bash
# Откатить к последнему рабочему коммиту
git revert HEAD
git push origin main
```

3. **Или верни Dockerfile вручную:**
```dockerfile
CMD ["node", "smart-ai-bot.js"]
```

---

## 📝 ИСТОРИЯ ПРОБЛЕМ (чтобы не повторять)

### 30.11.2025 - Ошибка деплоя
**Проблема:** Агент изменил `CMD` в Dockerfile на новый файл `phuket-go-bot-webhook.js` который имел ошибку парсинга Markdown.
**Решение:** Вернули `CMD ["node", "smart-ai-bot.js"]`
**Урок:** НЕ менять CMD без тестирования нового файла локально!

---

**ДАТА СОЗДАНИЯ:** 30 ноября 2025
**СТАТУС:** 🟢 Бот работает стабильно
**ВЕРСИЯ:** smart-ai-bot.js (AI бот с GPT)

